export class UsuariosService {
    static registrar(nombre: string, apellido: string, email: string, tipoUsuario: string, pass: string){
        //return nombre
        console.log(nombre + apellido + email + tipoUsuario + pass)
    }
}
